fastapi==0.111.0
uvicorn[standard]==0.30.1
motor==3.4.0
pymongo==4.7.3
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 relies on bcrypt's removed `__about__` attribute —
# bcrypt>=4.1 breaks password hashing entirely (ValueError on every hash).
# Pin to last compatible version until passlib is updated.
bcrypt==4.0.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4
email-validator==2.2.0
python-dotenv==1.0.1
httpx==0.27.0
watchdog==4.0.1
